#include <iostream>
#include <fstream>
#include <sstream>
#include <vector>
#include <string>
#include "func.h"
using namespace std;

int main()
{
	//// Create delta file
	//ifstream old("oldfile.txt");
	//ifstream new("newfile.txt");
	//ofstream delta("delta.txt");
	//createDelta(old, new, delta);

	//// Apply delta file
	//ifstream delta2("delta.txt");
	//ofstream applyhere("file.txt");
	//applyDelta(old, delta2, applyhere);

	return 0;
}